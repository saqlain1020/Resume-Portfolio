import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
  palette: {
      primary:{
          main: "rgb(100,20,20)",
          dark: "rgb(100,100,30)"
      },
      type: 'dark'
  },
});

export default theme;
